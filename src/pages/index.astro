---
import Layout from '../layouts/layout.astro'
import Button from '../components/Button.astro'

const content = {
  title: 'Soham Sarkar',
  description: `Soham's personal site`,
}
---

<Layout content={content}>
  <section class='hero-section'>
    <div class='hero-text'>
      <h1>
        I'm a <span>digital gardener</span>, and I
        <span class='italic-text'>craft impactful software experiences</span>
        with code.
      </h1>

      <p>
        I'll grow your ideas from tiny seeds into towering trees 🌱.<br /> Already have a garden? Let's
        transform it into a thriving forest 🌲.
      </p>
      <Button href='https://cal.com/soham-sarkar-work'>Let's grow together!</Button>
    </div>
  </section>

  <style>
    .hero-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;

      min-height: 65dvh;
      background: radial-gradient(circle, rgba(0, 0, 0, 0.7) 0%, rgba(0, 0, 0, 0.9) 100%);
      color: #ffffff;
    }

    .hero-text {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 24px;
      width: 100%;
    }

    h1 {
      color: #fff;
      font-size: 4.5rem;
      font-weight: 400;
      line-height: 100%;
      letter-spacing: -0.36rem;
    }

    .italic-text {
      font-style: italic;
      color: #7f7f7f;
    }

    p {
      font-size: 1.25rem;
      font-weight: 500;
      color: #71717a;
    }

    .cta-button {
      margin-top: 20px;
      padding: 15px 30px;
      font-size: 1.25rem;
      font-weight: 600;
      color: #ffffff;
      border: 1px solid rgba(255, 255, 255, 0.6);
      border-radius: 40px;
      background: transparent;
      text-decoration: none;
      box-shadow: 0px 0px 24px rgba(255, 255, 255, 0.34);
      transition: all 0.3s ease;
    }

    .cta-button:hover {
      background: rgba(255, 255, 255, 0.1);
      box-shadow: 0px 0px 30px rgba(255, 255, 255, 0.5);
    }

    @media (max-width: 768px) {
    }

    @media (max-width: 480px) {
    }
  </style>
</Layout>
